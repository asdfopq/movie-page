import React from "react";
import styled from "styled-components";

const CharactersContainer = styled.div`
  font-size: 32px;
`;

const Characters = () => {
  return <CharactersContainer>Characters</CharactersContainer>;
};

export default Characters;
